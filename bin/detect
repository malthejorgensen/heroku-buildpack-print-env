#!/usr/bin/env bash

BP_DIR=$(cd $(dirname $0); cd ..; pwd)

# this pack is valid for apps with a hello.txt in the root
if [ -f $BP_DIR/export ]; then
  echo 'Hello from the "print-exports" buildpack!'
  exit 0
else
  exit 1
fi
